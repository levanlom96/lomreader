{
  "name": "@lomreader/integration",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 5174",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:install-browsers": "playwright install chromium",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "lomreader": "workspace:*"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
